{
  "name": "roman-converter",
  "version": "1.0.0",
  "description": "Roman numeral converter with automated tests",
  "main": "converter.js",
  "scripts": {
    "test": "mocha-headless-chrome -f test.html --args no-sandbox"
  },
  "devDependencies": {
    "mocha-headless-chrome": "^4.0.0"
  }
}
